name: nxdesign_example
description: "Demonstrates how to use the nxdesign plugin."
publish_to: "none"

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  nxdesign:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
